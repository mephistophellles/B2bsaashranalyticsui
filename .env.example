# Frontend (Vite) — optional; default is proxy /api -> http://127.0.0.1:8000 (dev)
# VITE_API_BASE_URL=/api
# Продакшен: полный URL API с суффиксом /api (роуты клиента: /auth/login → …/api/auth/login)
# VITE_API_BASE_URL=https://api.example.com/api
